files:
  "/opt/elasticbeanstalk/hooks/appdeploy/post/01_npm_install.sh":
    mode: "000755"
    owner: root
    group: root
    content: |
      #!/bin/bash
      # sudo docker exec `sudo docker ps --filter "name=frontend" --format "{{.Names}}"` npm ci
      echo `date`
